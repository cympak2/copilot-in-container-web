# Path to the cic (copilot-in-container) executable
# If not set, will use 'cic' from PATH
# CIC_PATH=/path/to/cic

# Alternative: Create ~/.copilot-in-container/web-config.json with:
# {
#   "cicPath": "/path/to/cic"
# }
